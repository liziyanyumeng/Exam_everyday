<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lliziyan20200320">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Order0402.OrderActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".show0327.CreateActivity" />
        <activity android:name=".show0327.DingdanActivity" />
        <activity android:name=".show0327.LeimuActivity">
        </activity>
        <activity android:name=".Show0326.ShopCarActivity" />
        <activity android:name=".Show0325.NewActivity" />
        <activity android:name=".QrCode.MainActivity" />
    </application>

</manifest>